# static (reference only)

This directory preserves the legacy pre-WordPress static source as historical reference.

## Important
- Do not deploy from `static/`.
- Do not treat files here as current production templates.
- Active implementation lives in `wp/wp-content/themes/nor/`.

## Usage
- Compare old markup/content when needed.
- Reuse assets cautiously and only after confirming current theme compatibility.
